# Jemshorts Agent Contract

이 저장소에서 커머스 콘텐츠를 조사·렌더·게시하는 모든 에이전트와 LLM은 아래 계약을 따른다.

## 발행 단위

- 공개 커머스 콘텐츠는 동일 검색 의도의 검증 상품 **3개 또는 5개**를 포함해야 한다.
- 일일 기본은 TOP3, 주간 결산은 TOP5다.
- 단일 상품 및 2개 상품 콘텐츠는 렌더·블로그·클립·게시 모두 금지한다.
- 서로 무관한 상품을 단순 성과순으로 섞지 않는다. `products.json.collections`의 활성 컬렉션만 사용한다.

## 근거와 품질

- 상품별 식별자, 구성 수량, 대표 이미지, 판매처 링크, 가격 확인일을 독립 검증한다.
- 가격은 3일 이내 확인값만 현재가/최저가로 표현한다.
- 허위 구매 경험, 조작된 커뮤니티 사연, 가짜 리뷰를 만들지 않는다.
- 실제 가격 차이, 옵션 차이, 출처가 확인된 반복 리뷰 불만은 훅으로 사용할 수 있다.
- 대표 이미지와 렌더는 기술 QA와 시각 QA를 모두 통과해야 한다.

## 발행 게이트

- `app.promo.commerce_policy.validate_publication`을 통과하지 않은 공개 발행은 금지한다.
- `[광고]`와 사용 중인 모든 제휴 프로그램의 필수 경제적 이해관계 문구를 콘텐츠 상단에 표시한다.
- 게시 후 공개 URL, 상품 수, 링크, 고지, 이미지/영상 상태를 다시 조회해 검증한다.

## 성과

- 조회수보다 클릭, 주문, 실제 수수료, EPC를 우선한다.
- 클릭 0인 채널에 같은 포맷을 반복 게시하지 않는다.
- 상품·오퍼·성과는 `storage/promo.db`와 `product-hub/products.json`을 단일 원장으로 사용한다.
